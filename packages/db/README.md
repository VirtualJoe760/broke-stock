# broke db

Drizzle ORM schema + migrations for app state on Postgres/Neon ([persistence & events](../../docs/01-architecture/persistence-and-events.md)).

Tables (draft): `accounts`, `strategies`, `orders`, `positions`, `fills`, `audit`.

> Placeholder. Schema is authored in a later Phase 0 pass. Time-series (ticks) live in QuestDB/DuckDB, not here.
